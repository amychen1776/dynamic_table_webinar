models:
  - name: stg_flight_location
    description: staging model for our flight location 
    columns:
      - name: primary_key
        tests:
          - not_null
          - unique 
        
          